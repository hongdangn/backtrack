#include <stdio.h>
#include <stdlib.h>

int v;
int matrix[100][100];
bool visited[100];
void bfs(int u)
{
	
}
int main()
{
	scanf("%d", v);
	for(int i = 0; i < v; i++){
		for(int j = 0; j < v; j++){
			scanf("%d", &matrix[i][j]);
		}
	}
}
